// Comentário de linha no JS

/* Comentário de várias 
linhas no JS */

var x = 1; //Variável
var y = 2;
var z = 3;

//var preco = 15;
var desconto = 0.5;
const PRECO = 15; //Constante (O valor é fixo)
var total = PRECO - desconto;

//preco = preco - desconto;

console.log("Hello World!");
